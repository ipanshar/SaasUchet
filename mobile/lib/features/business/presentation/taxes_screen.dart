part of 'business_shell.dart';

class _TaxesScreen extends StatelessWidget {
  const _TaxesScreen();

  @override
  Widget build(BuildContext context) {
    return const _PlaceholderScreen(
      icon: Icons.receipt_long_rounded,
      title: 'Налоги',
      color: Color(0xFFEF4444),
    );
  }
}
